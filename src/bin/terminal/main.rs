use memo::{start_game, TerminalPresenter, TerminalPrompter};



fn main() {
    println!("Hello World!");

    let prompter = TerminalPrompter {

    };

    let presenter = TerminalPresenter {

    };

    start_game(prompter, presenter);
}